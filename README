这是一起商层架构的失败的尝试:
1.系统按功能模块分层，导致重复写了很多轮子，重复写了很多xml
2.因为分层问题不正确的打包，导致不能实现分布式服务架构
3.mysql的读写分离使用的是aop来自动切换数据库，这样就会导致每次操作数据库的时候都要判断方法名切换数据库，导致操作数据库很慢，还不如读写分离
4.就是不写注释这件事，我到现在都还没有改的掉，下次努力

注意：spring-data-mongodb 1.8.1.RELEASE 必须配spring 4.2.0.RELEASE以上的版本，不然会出现系统兼容
